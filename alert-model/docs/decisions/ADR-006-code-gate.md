# ADR-006 闸门用代码

- 状态：已接受
- 日期：2026-08-17

## 背景

全量告警进大模型会在输出 token、费用、SLA 上失败。小模型能做分堆，但排除、指纹、复用、缺字段、高危加塞用规则即可。

## 决定

告警写入后先跑确定逻辑，结果写成 `analysis_type=GATE` 的一行（`next_hop` + `noise_reason`）。

不在第一版引入分类小模型。命令行指纹先规范化再哈希。中间地带（`next_hop=FULL_AGENT` 或 `HUMAN`）才进大模型或人。

租户排除名单和 FP 指纹是服务配置表，不进告警语义主模型。
