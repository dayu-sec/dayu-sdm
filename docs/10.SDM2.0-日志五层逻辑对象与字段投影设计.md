# SDM2.0 日志五层逻辑对象与字段投影设计

> **文档定位：** 将当时 `sdm_event` 的 87 个标准字段组织为稳定的逻辑对象，定义逻辑路径到 Doris 物理列的投影关系，并给出目标物理模型的增量改造要求。
>
> **适用范围：** SDM2.0 日志事件、来源侧 Finding、日志接入映射、查询 API 和 Doris 查询投影。
>
> **状态：** 设计建议稿。可执行中间版已落地为
> `log-model/schema/007_sdm_event.sql`
> 与 `log-model/contracts/hybrid-event/`。本文不替代这两处。
>
> **当时基线：** `log-model/docs/main/05-sdm-event-logical-field-catalog.md` 和 `log-model/schema/007_sdm_event.sql`。


---

## 一、结论

当前 `sdm_event` 的 87 个标准字段都可以获得明确的逻辑落位，但不能据此认为当前宽表能够无损承载或还原完整逻辑对象。五个语义层为：

1. `metadata`：记录身份、时间、数据来源、日志来源和原始引用；
2. `event`：事件类型、动作、结果、严重度和消息；
3. `roles`：事件参与者及其角色；
4. `facets`：网络、协议、应用和运行环境等行为切面；
5. `source_finding`：来源产品自带的检测或告警判断。

`extensions` 是五个语义层之外的兼容容器，不是第六个业务语义层。它只承载尚未标准化、厂商私有或需要兼容保留的结构化字段。

逻辑对象是权威语义，当前 87 个 Doris 列是查询友好的兼容物理投影。两者不要求存储形态相同：逻辑层保持对象、角色和多值语义，物理层可以为过滤、聚合和关联保留展平列。`PRIMARY_ONLY`、`DERIVED`、`REFERENCE_ONLY` 和 `NOT_PROJECTED` 都意味着逻辑对象到主宽表的投影可能有损；完整语义必须由逻辑对象存储、受治理的 domain `VARIANT` 或关系明细表承载。

~~~text
原始日志
   ↓ 来源映射与归一化
SDM 五层逻辑事件
   ↓ projection contract
sdm_event 87 列查询投影
   ↓ 派生
实体索引 / 关系表 / 告警证据
~~~

---

## 二、设计原则

### 2.1 一个事实只有一个逻辑权威位置

同一事实可以为 Doris 查询冗余投影，但不能拥有两套相互独立的逻辑语义。例如：

| 事实 | 逻辑权威位置 | 当前查询投影 |
|---|---|---|
| 发起端 IP | `roles.source.endpoint.ip` | `source_ip` |
| HTTP 请求方法 | `facets.http.request.method` | `http_method` |
| 被访问文件路径 | `roles.target.file.path` | `target_file_path` |
| 来源检测标题 | `source_finding.title` | `source_alert_name` |

### 2.2 角色与实体类型分离

`source`、`carrier`、`target`、`observer`、`related` 是事件角色；`user`、`account`、`host`、`process`、`file`、`service` 和 `endpoint` 是实体类型。实体必须放在角色中，不能默认与角色并列。

同一种实体可以在不同事件中承担不同角色。例如文件可以是邮件附件对应的 carrier，也可以是文件写入事件的 target；进程可以是行为发起者、执行载体或被创建的目标进程。

### 2.3 协议事实不伪装成角色实体

协议、方向、会话、HTTP、数据库和邮件描述行为如何发生，属于 `facets`。它们可以与角色对象组合，但不能因为当前物理列带有 `carrier_` 前缀就永久归入 carrier 实体。

实体类型、值对象和行为切面的完整注册表见 [docs/2.实体清单.md](2.实体清单.md)。当前统一实体命名使用 `process`，云资源统一使用 `cloud`；`thread`、`module` 作为进程属性或相关进程表达，不作为并列核心实体。

### 2.4 来源 Finding 与平台 Alert 分离

`source_finding` 只表达上游产品对这条观测事实作出的检测判断。平台统一 Alert 的聚合、去重、分派、研判、工单和处置状态继续由 `sdm_alert*` 承载，并通过证据关系引用事件。

### 2.5 逻辑多值不受物理单值限制

逻辑层中的 `roles.carriers`、`roles.related`、邮件收件人、附件、漏洞、IOC 和证据引用应支持数组。当前宽表中的单值列只保存主值或查询投影，不应反向限制逻辑模型。

### 2.6 数据来源与事件 source 分离

`metadata.data_source` 表示数据接入来源、厂商、产品和采集实例，回答“这条记录从哪里进入平台”；`roles.source` 表示事件行为的主动方，回答“谁发起了这个行为”。`roles.observer` 表示观察、检测或上报该事件的设备或产品。

三者可能指向同一产品或设备，也可能完全不同。映射器不得因为名称或取值相同而自动互相复制，必须根据来源契约和事件类型契约分别判断。

### 2.7 逻辑权威与物理投影单向生成

目标 Doris 主表采用“稳定标量列 + 完整逻辑对象 `VARIANT`”的混合结构，但不能因此形成两套独立事实：

- `metadata`、`event` 的稳定标量字段直接存为物理列，由 API 组装为逻辑对象；
- `roles`、`facets`、`source_finding` 的完整对象是这些语义层的逻辑权威存储；
- `source_ip`、`target_port`、`http_method`、`source_finding_severity` 等顶层列是从逻辑对象单向生成的查询投影；
- `extensions` 是独立的受治理兼容容器，不得覆盖标准对象或投影列。

写入链路必须为：

~~~text
来源字段
  → 五层逻辑对象
  → Schema 校验、归一化和角色判断
  ├─ 写入完整 roles/facets/source_finding/extensions VARIANT
  └─ 按 projection registry 生成顶层查询列
~~~

禁止由来源字段分别生成 JSON 和顶层列。`mapping_revision` 记录来源到逻辑路径的映射版本，`projection_version` 记录逻辑路径到物理列、实体 posting 和关系明细的派生规则版本。写入器必须在落库前校验两者一致性。

---

## 三、目标逻辑结构

~~~json
{
  "metadata": {
    "schema_version": "",
    "mapping_id": "",
    "mapping_revision": "",
    "projection_version": "",
    "quality_status": "",
    "tenant_id": "",
    "event_id": "",
    "log_id": "",
    "occur_time": "",
    "ingest_time": "",
    "parse_time": "",
    "data_source": {
      "vendor": "",
      "product": "",
      "category": "",
      "instance_id": ""
    },
    "log": {
      "type": "",
      "level": "",
      "name": ""
    },
    "raw_log_id": "",
    "original_event_id": ""
  },
  "event": {
    "record_kind": "",
    "domain": "",
    "type": "",
    "operation": "",
    "outcome": "",
    "severity": "",
    "message": ""
  },
  "roles": {
    "source": {},
    "carriers": [],
    "target": {},
    "observer": {},
    "related": []
  },
  "facets": {
    "network": {},
    "application": {},
    "http": {},
    "database": {},
    "email": {},
    "container": {}
  },
  "source_finding": {},
  "extensions": {
    "schema_version": "1",
    "source_private": {},
    "profiles": {},
    "enrichments": {},
    "unmapped": {}
  }
}
~~~

`facets` 的对象集合不是封闭枚举。后续可以按受治理的 Schema 增加 `dns`、`tls`、`authentication`、`file_activity`、`registry`、`cloud` 和 `ot` 等稳定行为切面，而不改变五层结构。

`roles` 中的实体直接放在其事件角色下面，`facets` 中的对象直接按行为域存储，不增加顶层 `entities[]`，也不为每个对象统一增加位置索引。需要事件内交叉引用的数组成员使用稳定 `ref_id`；`role_index`、`relation_index` 等确定性序号在派生 posting 或关系表时生成，不作为逻辑对象身份。

---

## 四、87 字段覆盖核对

| 逻辑位置 | 当前字段范围 | 字段数 |
|---|---|---:|
| `metadata` | 身份、时间、来源、日志和原始引用 | 15 |
| `event` | 类型、分类、动作、结果、严重度和消息 | 6 |
| `roles` | source、carrier process、target、observer/device | 36 |
| `facets` | 网络、NAT、应用、HTTP、数据库、邮件和容器 | 23 |
| `source_finding` | `source_alert_*` | 6 |
| `extensions` | `extension` | 1 |
| **合计** |  | **87** |

字段数核对只证明当前物理字段都有逻辑落位，不证明投影可逆，也不代表大禹历史全部字段或未来日志类型已经完整覆盖。新增语义仍需进入对象 Schema、事件类型契约和投影契约评审。

### 4.1 当前列与目标列必须分开管理

| 物理契约 | 字段数 | 用途 |
|---|---:|---|
| 当前兼容契约 | 87 | 保持现有接入、查询和 API 兼容 |
| P0 兼容增量契约 | 89 | 在 87 列基础上增加 `record_kind`、`event_domain` |
| 混合存储目标契约 | 动态评审 | 稳定标量列、经查询证明的投影列和 4 个受治理 `VARIANT`；不以固定字段数为目标 |

“87 字段覆盖”是现状核对，不是目标模型的字段上限。新增的两个分类列属于必要的查询维度，不应放入 `extension`，也不应只在 API 查询时临时派生。

兼容迁移期允许当前 87 列与目标混合结构并存。目标命名与历史命名不一致时，通过兼容视图或 API 别名提供旧字段，例如 `network_protocol → carrier_protocol`、`source_finding_severity → source_alert_severity`、`extensions → extension`；不得让两个名称成为可独立写入的物理事实。

### 4.2 物理字段的语义分类

物理字段首先按唯一语义归属分组。该分类用于字段标准、projection registry、权限和 API 解释，覆盖当前全部 87 列且组间不重复：

| 物理语义组 | 当前字段 | 数量 | 主要逻辑归属 |
|---|---|---:|---|
| 记录控制与追溯 | `tenant_id`、`occur_time`、`event_id`、`log_id`、`ingest_time`、`parse_time`、`raw_log_id`、`source_original_event_id` | 8 | `metadata` |
| 数据来源与日志描述 | `data_src_*`、`log_type`、`log_level`、`log_name` | 7 | `metadata.data_source`、`metadata.log` |
| 事件事实 | `event_type`、`event_category`、`operation`、`outcome`、`severity`、`log_content` | 6 | `event`；其中 `event_category` 为兼容字段 |
| source 角色 | `source_user`、`source_account`、`source_host`、`source_ip`、`source_port`、`source_mac`、`source_process_name` | 7 | `roles.source` |
| primary carrier 角色 | `carrier_process_name`、`carrier_process_pid`、`carrier_process_guid`、`carrier_process_cmdline`、`carrier_process_path` | 5 | `roles.carriers[]` 的主值投影 |
| target 角色 | `target_user`、`target_host`、`target_ip`、`target_port`、`target_mac`、`target_service`、`target_domain`、`target_url`、`target_file_*` | 14 | `roles.target` |
| observer 与设备 | `observer_*`、`device_*` | 10 | `roles.observer` |
| 行为切面 | `carrier_protocol`、`carrier_app_name`、`carrier_direction`、`carrier_session_id`、`nat_*`、`http_*`、`db_*`、`sql_statement`、`mail_*`、`k8s_*` | 23 | `facets` |
| 来源 Finding | `source_alert_*` | 6 | `source_finding` |
| 受治理扩展 | `extension` | 1 | `extensions` envelope |
| **合计** |  | **87** |  |

P0 新增的 `record_kind`、`event_domain` 归入“事件事实”，因此目标 89 列中该组由 6 列增加到 8 列。`carrier_protocol/app_name/direction/session_id` 虽然保留历史 `carrier_` 前缀，但物理语义分类归入行为切面，不归入 carrier 实体。

### 4.3 Doris 访问职责分类

语义分类之外，每个字段还应声明一个或多个物理访问标签。访问标签用于决定类型、索引和存储位置，不改变字段的逻辑权威路径：

| 访问标签 | 用途 | 典型字段或对象 | 物理策略 |
|---|---|---|---|
| `KEY` | 唯一性、租户隔离、分区 | `tenant_id`、`occur_time`、`event_id` | Unique Key、分区键、分桶键 |
| `LOOKUP` | ID 点查和证据回溯 | `log_id`、`raw_log_id`、`source_original_event_id`、`source_alert_original_id` | Bloom Filter 或 INVERTED，按查询基准选择 |
| `DIMENSION` | 高频过滤、分组和关联 | `record_kind`、`event_domain`、`event_type`、IP、用户、observer、规则 ID | 稳定标量列；必要时 INVERTED |
| `MEASURE` | 范围过滤或统计 | 端口、HTTP 状态、文件大小 | 数值列；不默认建立倒排索引 |
| `DETAIL` | 列表展示、调查上下文 | URL、命令行、SQL、消息、邮件主题 | `TEXT` 或有界 `VARCHAR`；限制大小和敏感数据 |
| `SPARSE` | 稳定但低频的对象详情 | 受治理的 domain 对象 | 有 Schema 的 `VARIANT` |
| `RELATION` | 多值实体和反向检索 | carriers、related、收件人、附件、IOC、CVE、证据 | 窄明细表或实体 posting 表，不压入标量列 |
| `RAW_REFERENCE` | 原文、Payload、PCAP 回查 | `raw_log_id` 及证据引用 | 主表只存稳定引用，内容外置 |

这两个分类维度不能合并。例如 `source_ip` 的语义组是“source 角色”，访问标签是 `DIMENSION`；`target_url` 的语义组是“target 角色”，访问标签通常是 `DETAIL`；完整邮件附件的语义组是“行为切面”，访问标签是 `RELATION`。

---

## 五、metadata 字段投影

`metadata` 描述事件记录本身，不参与行为角色判断。

| 当前物理字段 | 逻辑权威路径 | 说明 |
|---|---|---|
| `tenant_id` | `metadata.tenant_id` | 租户或空间编号 |
| `event_id` | `metadata.event_id` | SDM 事件唯一标识 |
| `log_id` | `metadata.log_id` | 来源日志记录标识 |
| `occur_time` | `metadata.occur_time` | 事件发生时间 |
| `ingest_time` | `metadata.ingest_time` | 平台接收或写入时间 |
| `parse_time` | `metadata.parse_time` | 解析完成时间 |
| `data_src_vendor` | `metadata.data_source.vendor` | 数据来源厂商 |
| `data_src_product` | `metadata.data_source.product` | 数据来源产品 |
| `data_src_category` | `metadata.data_source.category` | 数据来源分类 |
| `data_src_instance_id` | `metadata.data_source.instance_id` | 数据来源实例、探针或采集点 |
| `log_type` | `metadata.log.type` | 稳定日志类型或子类型 |
| `log_level` | `metadata.log.level` | 来源日志等级 |
| `log_name` | `metadata.log.name` | 来源日志名称 |
| `raw_log_id` | `metadata.raw_log_id` | 平台原始日志库稳定主键 |
| `source_original_event_id` | `metadata.original_event_id` | 来源系统原始事件 ID |

目标逻辑 Schema 还应增加 `metadata.schema_version`、`metadata.mapping_id`、`metadata.mapping_revision` 和数据质量状态。它们目前不是 87 列的一部分，是否物化为 Doris 顶层列需单独评审。

---

## 六、event 字段投影

`event` 描述发生了什么，不保存参与者，也不保存来源产品或平台对事件的安全判断。

| 当前物理字段 | 逻辑权威路径 | 说明 |
|---|---|---|
| `event_type` | `event.type` | 标准事件类型 |
| `event_category` | `event.legacy_category` | 当前兼容分类，不再作为目标 canonical 分类 |
| `operation` | `event.operation` | 细粒度动作 |
| `outcome` | `event.outcome` | 行为结果 |
| `severity` | `event.severity` | 事件层严重度 |
| `log_content` | `event.message` | 简短内容或摘要，不复制原文 |

### 6.1 分类维度修正

当前 `event_category` 的 `auth/network/audit/system/alert/other` 将行为领域与记录性质混合。`alert` 表示来源声称这是一条 Finding，而 `network` 表示行为领域，两者并不正交。

目标逻辑层建议拆分：

| 逻辑路径 | 回答的问题 | 示例 |
|---|---|---|
| `event.record_kind` | 这是什么性质的记录 | `activity`、`finding`、`inventory`、`state`、`remediation` |
| `event.domain` | 属于哪个行为领域 | `identity`、`network`、`system`、`application`、`discovery` |
| `event.type` | 具体发生了什么 | `authentication`、`process_launch`、`network_connection` |
| `event.operation` | 该类型下的具体动作 | `logon`、`exec`、`connect` |
| `event.outcome` | 动作结果 | `success`、`failed`、`unknown` |

P0 目标物理字段定义为：

| 目标物理字段 | 类型 | 逻辑权威路径 | 索引 | 说明 |
|---|---|---|---|---|
| `record_kind` | `VARCHAR(32)` | `event.record_kind` | INVERTED | `activity/finding/inventory/state/remediation` |
| `event_domain` | `VARCHAR(64)` | `event.domain` | INVERTED | `identity/network/system/application/discovery/...` |

兼容期旧名 `event_category` 仅由只读视图/API alias 投影到 `event_domain`（registry 定义 `event_category → event_domain, read_only: true`；version-policy `legacy_alias_write=reject`）——**新 writer 只写 `event_domain`，禁止写入旧物理列**。`event_category` 不能作为 `record_kind` 或 `event_domain` 的唯一数据源；历史数据迁移规则必须显式声明，无法确定时留空并记录数据质量状态。

### 6.2 严重度与结果

`event.severity` 与 `source_finding.severity` 是不同枚举族，不得因为来源只有一个“严重度”字段而无条件双写：

- `event.severity` 使用事件层 syslog 小写八级：`emerg/alert/crit/error/warning/notice/info/debug`；
- `source_finding.severity` 保存来源检测判断的严重度，如 `critical/high/medium/low/informational`；
- 来源告警的“高危”不能直接写入 `event.severity`，除非事件类型契约定义了可审计的确定性映射；
- `event.outcome` 只描述行为结果，不保存阻断动作、处置状态或检测结论。

---

## 七、roles 字段投影

### 7.1 source

| 当前物理字段 | 逻辑权威路径 |
|---|---|
| `source_user` | `roles.source.user.name` |
| `source_account` | `roles.source.account.name` |
| `source_host` | `roles.source.host.name` |
| `source_ip` | `roles.source.endpoint.ip` |
| `source_port` | `roles.source.endpoint.port` |
| `source_mac` | `roles.source.host.mac` |
| `source_process_name` | `roles.source.process.name` |

`source.user` 表达自然人或业务身份，`source.account` 表达系统、应用或服务账号。来源无法证明两者差异时不得无条件双写。

`source.host.mac` 表达来源主机的资产 MAC；`source.endpoint.mac` 仅表达网络会话端点 MAC，两者不得机械双写。

### 7.2 carriers

| 当前物理字段 | 逻辑权威路径 |
|---|---|
| `carrier_process_name` | `roles.carriers[].process.name`（`PRIMARY_ONLY`） |
| `carrier_process_pid` | `roles.carriers[].process.pid`（`PRIMARY_ONLY`） |
| `carrier_process_guid` | `roles.carriers[].process.uid`（`PRIMARY_ONLY`） |
| `carrier_process_cmdline` | `roles.carriers[].process.cmdline`（`PRIMARY_ONLY`） |
| `carrier_process_path` | `roles.carriers[].process.path`（`PRIMARY_ONLY`） |

逻辑层使用数组是为了表达父进程、启动器、脚本、附件和多级代理等多个载体。当前列只投影一个 primary carrier。`[primary]` 不是合法的逻辑路径或 JSONPath，primary 是投影策略：主载体选择规则必须由 `event.type + metadata.log.type` 对应的事件类型契约确定，并在映射版本中固定。多个 carrier 需要检索时写入实体或关系明细表。

### 7.3 target

| 当前物理字段 | 逻辑权威路径 |
|---|---|
| `target_user` | `roles.target.user.name` |
| `target_host` | `roles.target.host.name` |
| `target_ip` | `roles.target.endpoint.ip` |
| `target_port` | `roles.target.endpoint.port` |
| `target_mac` | `roles.target.endpoint.mac` |
| `target_service` | `roles.target.service.name` |
| `target_domain` | `roles.target.domain.name` |
| `target_url` | `roles.target.url.full` |
| `target_file_path` | `roles.target.file.path` |
| `target_file_name` | `roles.target.file.name` |
| `target_file_md5` | `roles.target.file.hashes.md5` |
| `target_file_sha1` | `roles.target.file.hashes.sha1` |
| `target_file_sha256` | `roles.target.file.hashes.sha256` |
| `target_file_size` | `roles.target.file.size` |

`target_domain` 可以是来源直接提供的权威值，也可以是查询投影的派生值。生成物理列时使用固定优先级：

1. 来源明确提供且通过域名校验的 `roles.target.domain.name`；
2. 从 `roles.target.url.full` 解析出的 host；
3. `facets.http.request.host`；
4. 事件类型契约允许使用的 DNS 查询名。

来源直接值与派生值冲突时保留直接值，记录冲突和派生候选，不得由查询层临时覆盖。projection registry 必须按具体映射声明 `DIRECT` 或 `DERIVED`，不能把 `target_domain` 全局固定为其中一种。

### 7.4 observer

| 当前物理字段 | 逻辑权威路径 |
|---|---|
| `observer_vendor` | `roles.observer.product.vendor` |
| `observer_product` | `roles.observer.product.name` |
| `observer_type` | `roles.observer.type` |
| `device_name` | `roles.observer.device.name` |
| `device_ip` | `roles.observer.device.ip_addresses[]`（主管理地址投影） |
| `device_ipv4` | `roles.observer.device.ip_addresses[]`（主 IPv4 投影） |
| `device_ipv6` | `roles.observer.device.ip_addresses[]`（主 IPv6 投影） |
| `device_mac` | `roles.observer.device.mac` |
| `device_serial_num` | `roles.observer.device.serial_number` |
| `device_model` | `roles.observer.device.model` |

将 `device_*` 放入 observer 是当前字段定义下的默认投影，而不是所有日志类型的机械规则：

- 防火墙、WAF、探针记录外部行为时，设备通常是 observer；
- 主机自身的系统日志中，设备也可能是 source host 或 target host；
- 同一个设备同时承担观察者和参与者时，逻辑层可以表达两个角色，物理投影不得通过无条件复制制造虚假的角色事实。

设备地址的逻辑权威结构统一为数组，例如：

~~~json
{
  "ip_addresses": [
    {"address": "192.0.2.10", "version": 4, "usage": "management"},
    {"address": "2001:db8::10", "version": 6, "usage": "management"}
  ]
}
~~~

三个兼容物理列不是三套独立语义：`device_ip` 保存事件类型契约选定的主管理地址，`device_ipv4/device_ipv6` 分别保存对应版本的主值。出现冲突时不得静默覆盖，且 projection registry 应标记为 `PRIMARY_ONLY`，而不是普通直接映射。

### 7.5 related

当前 87 列没有 `related[]` 的直接物理列，但逻辑层必须保留该角色，用于 DNS 应答 IP、额外文件、DLL、证书、IOC 指向对象和其他非主行为链实体。

需要按实体、IOC、CVE 等字段反向检索的 related 对象，应展开到实体或关系索引；不应默认塞入无 Schema 的 `extension`。

---

## 八、facets 字段投影

### 8.1 network 与 application

| 当前物理字段 | 逻辑权威路径 |
|---|---|
| `carrier_protocol` | `facets.network.protocol` |
| `carrier_direction` | `facets.network.direction` |
| `carrier_session_id` | `facets.network.session_id` |
| `carrier_app_name` | `facets.application.name` |
| `nat_source_ip` | 由来源契约映射到 `facets.network.nat.original/translated.*` |
| `nat_source_port` | 由来源契约映射到 `facets.network.nat.original/translated.*` |
| `nat_target_ip` | 由来源契约映射到 `facets.network.nat.original/translated.*` |
| `nat_target_port` | 由来源契约映射到 `facets.network.nat.original/translated.*` |

当前 `carrier_*` 列名继续作为兼容物理投影。逻辑层将协议、方向、会话和应用移入 facets，避免把协议或会话当成实体角色。

NAT 的逻辑权威结构必须区分 `original.source`、`original.target`、`translated.source` 和 `translated.target`，并记录观测方向。仅凭 `nat_source/nat_target` 名称不能覆盖所有防火墙产品的 NAT 语义。来源契约未能证明转换前后关系时，四个兼容列不得写入，原值进入受治理的 `extensions.unmapped` 并标记 `NEEDS_REVIEW`。

### 8.2 HTTP

| 当前物理字段 | 逻辑权威路径 |
|---|---|
| `http_method` | `facets.http.request.method` |
| `http_host` | `facets.http.request.host` |
| `http_user_agent` | `facets.http.request.user_agent` |
| `http_referer` | `facets.http.request.referer` |
| `http_status` | `facets.http.response.status_code` |

`roles.target.url.full` 是目标资源，`facets.http.request.*` 是 HTTP 协议事实。`target_domain` 的直接值和派生优先级遵循 7.3 节，不能由下游临时猜测。

### 8.3 database

| 当前物理字段 | 逻辑权威路径 |
|---|---|
| `db_name` | `facets.database.name` |
| `db_type` | `facets.database.type` |
| `db_user` | `facets.database.user.name` |
| `sql_statement` | `facets.database.statement` |

数据库服务实体仍可出现在 `roles.target.service`。数据库名称、用户和 SQL 描述数据库行为细节，归入 database facet。

### 8.4 email

| 当前物理字段 | 逻辑权威路径 |
|---|---|
| `mail_sender` | `facets.email.sender` |
| `mail_receiver` | `facets.email.recipients[]`（`PRIMARY_ONLY`） |
| `mail_subject` | `facets.email.subject` |
| `mail_attachment_name` | `facets.email.attachments[].name`（`PRIMARY_ONLY`） |

`mail_receiver` 和 `mail_attachment_name` 当前为单个 `TEXT`，逻辑层必须按数组表达。为避免分隔符、转义和反向解析歧义，当前宽表统一只保存事件类型契约选定的主收件人和主附件名，projection registry 标记为 `PRIMARY_ONLY`。完整数组保存在逻辑对象存储；需要按任意收件人或附件检索时，分别写入邮件参与者、附件关系明细表，不在 `TEXT` 中序列化数组。

### 8.5 container

| 当前物理字段 | 逻辑权威路径 |
|---|---|
| `k8s_pod_name` | `facets.container.kubernetes.pod.name` |
| `k8s_namespace` | `facets.container.kubernetes.namespace` |

容器本身作为参与资源时，也可以出现在 source、target 或 observer 的 resource 对象中。facet 保存本次事件的运行环境快照；角色中的 resource 表达该容器在行为中的参与身份。两者同时存在时必须引用同一稳定资源标识。

---

## 九、source_finding 字段投影

| 当前物理字段 | 逻辑权威路径 |
|---|---|
| `source_alert_name` | `source_finding.title` |
| `source_alert_severity` | `source_finding.severity` |
| `source_alert_category` | `source_finding.category` |
| `source_alert_signature_id` | `source_finding.rule.signature_id` |
| `source_alert_action` | `source_finding.action` |
| `source_alert_original_id` | `source_finding.original_id` |

目标结构建议为：

~~~json
{
  "source_finding": {
    "original_id": "",
    "type": "detection",
    "title": "",
    "description": "",
    "severity": "",
    "confidence": {},
    "risk": {},
    "category": "",
    "action": "",
    "rule": {
      "id": "",
      "signature_id": "",
      "name": "",
      "version": ""
    },
    "security_results": [],
    "affected_entities": [],
    "vulnerabilities": [],
    "indicators": [],
    "malware": [],
    "attacks": [],
    "evidence_refs": [],
    "remediation": {}
  }
}
~~~

来源 Finding 中的攻击者、受害者或受影响实体，是来源判断或研判角色，不得无条件覆盖事件中被实际观测到的 source/target 角色。平台 Alert 需要通过证据事件和规则重新确定 attacker/victim。

---

## 十、extensions 治理

| 当前物理字段 | 逻辑位置 |
|---|---|
| `extension` | 完整 `extensions` envelope，`VARIANT` |

`extensions` 不应继续作为无约束的兜底 JSON。目标逻辑至少区分：

| 分区 | 用途 |
|---|---|
| `extensions.source_private` | 厂商或产品私有但仍需保留的结构化字段 |
| `extensions.profiles` | 已注册的跨来源扩展对象；字段、类型和版本必须有 Schema |
| `extensions.enrichments` | Geo、资产、情报、组织等外部富化结果 |
| `extensions.unmapped` | 暂未完成语义映射的来源原值，必须可审计并设置治理期限 |

物理列 `extension` 必须保存固定 envelope，不允许直接把厂商字段放在根节点：

~~~json
{
  "schema_version": "1",
  "source_private": {
    "<vendor>.<product>": {}
  },
  "profiles": {
    "<profile_name>": {
      "schema_version": "1",
      "data": {}
    }
  },
  "enrichments": {
    "<provider>.<dataset>": {
      "schema_version": "1",
      "observed_at": "",
      "data": {}
    }
  },
  "unmapped": {
    "mapping_id": "",
    "expires_at": "",
    "fields": {}
  }
}
~~~

`extension` 的根节点只允许上述固定 key。命名空间、Schema 版本、值类型、大小限制和生命周期由 extension registry 管理。需要作为高频过滤、聚合或关联条件的字段必须提升为标准顶层列、受治理的 domain `VARIANT` 路径或关系明细列，业务规则不得依赖任意厂商私有路径。

OCSF Profile 是 Schema 组合机制，不是运行时数据桶。SDM 如需记录启用的 Profile，建议使用 `metadata.schema_profiles[]` 保存名称和版本，Profile 数据仍写入已注册的标准路径或 `extensions.profiles.<profile_name>`。

以下内容不得直接内联到 `extensions`：

- 完整原始日志；
- 大体积 Payload 或 PCAP；
- 已存在标准逻辑路径的重复字段；
- 未声明类型和版本的动态 key；
- 平台告警研判、工单和处置状态。

完整原文、Payload 和 PCAP 应保存稳定引用、Hash、大小和必要的证据元数据。

---

## 十一、逻辑与物理投影规则

### 11.1 写入方向

每个日志源映射必须先形成五层逻辑事件，再生成当前 Doris 查询投影。不得维护两套互不关联的字段解释。

~~~text
source field
  → canonical logical path
  → normalization / role decision
  → physical projection column
~~~

例如：

~~~text
vendor.src_ip
  → roles.source.endpoint.ip
  → IP 文本归一化
  → source_ip
~~~

### 11.2 投影类型

每条逻辑路径到物理列的映射必须声明以下类型之一：

| 类型 | 含义 |
|---|---|
| `DIRECT` | 逻辑值直接写入物理列 |
| `NORMALIZED` | 经过类型、大小写、时间、IP 或枚举归一化后写入 |
| `DERIVED` | 从一个或多个逻辑字段确定性生成 |
| `PRIMARY_ONLY` | 多值逻辑对象只投影确定的主值 |
| `REFERENCE_ONLY` | 主表只保存外部对象或证据引用 |
| `NOT_PROJECTED` | 逻辑层保留，但不进入 `sdm_event` 主表 |

投影类型只描述逻辑值如何进入物理模型，不描述来源字段是否完成语义映射。`MAPPED/NEEDS_REVIEW/OUT_OF_SCOPE` 等映射状态属于映射设计或质量报告，不能与投影类型合并，也不进入运行时事件。

### 11.3 禁止反向猜测

API 可以按投影契约从 Doris 列组装逻辑对象，但不得把缺失的逻辑关系凭列名猜出来。例如：

- 不能看到 `device_ip` 就自动同时生成 source host 和 observer device；
- 不能把 `source_ip` 自动认定为告警 attacker；
- 不能把 `http_host`、`target_domain` 和 URL host 的冲突值静默覆盖；
- 不能把单个 `mail_receiver` 字符串按未经声明的分隔符拆成数组。

### 11.4 冲突处理

逻辑权威值与派生投影冲突时：

1. 保留原始来源字段和映射版本；
2. 标记映射质量状态；
3. 按事件类型契约定义的优先级生成物理投影；
4. 禁止由查询层按每次请求临时决定优先级。

---

## 十二、事件类型契约

五层结构只能解决字段归属，不能单独解决父进程、附件、设备和目标对象的角色歧义。每个稳定 `event.type` 还需要机器可校验的事件类型契约，至少声明：

- 合法的 `event.operation`；
- 必填、推荐、可选和禁止的逻辑路径；
- source、carrier、target、observer、related 的唯一映射规则；
- 多值对象的主值选择规则；
- facets 的适用性；
- 来源 Finding 是否允许出现；
- 到当前 87 列及目标增量列的投影和冲突优先级；
- OCSF Class/Activity/Profile 兼容身份；
- Schema 和映射版本。

例如 `process_launch` 应明确：

~~~text
发起用户/主机        → roles.source
父进程或启动器       → roles.carriers[]
新创建进程           → roles.target.process
Agent/EDR            → roles.observer
进程启动结果         → event.outcome
来源 EDR 检测判断    → source_finding
~~~

---

## 十三、Doris 落地边界

本设计不建议将完整五层 JSON 作为 Doris 主表唯一存储方式，也不建议立即删除当前 87 个查询列。

推荐继续采用混合物理模型：

| 数据特征 | 物理落位 |
|---|---|
| 高频过滤、聚合和关联字段 | `sdm_event` 顶层物理列 |
| 稳定但稀疏的对象详情 | 受 Schema 约束的 domain `VARIANT` |
| 多值且需要反向检索的实体 | 实体 posting/索引表 |
| IOC、CVE、ATT&CK 和证据关系 | 窄明细关系表 |
| 原始日志、Payload、PCAP | 外部存储和稳定引用 |
| 平台告警运营状态 | `sdm_alert*` |

逻辑 API 可以组装五层对象；常用查询、规则和列表继续优先访问稳定物理列。业务方不应直接依赖厂商 `extension` 路径。

### 13.1 目标混合表结构

当前 87 列在迁移期保持兼容。目标主表将稳定的 `metadata/event` 字段、高频查询投影和完整逻辑对象组合存储。以下 DDL 是目标契约候选，不替代当前可执行基线；正式实施必须配套迁移、回填、兼容视图和基准验证：

~~~sql
CREATE TABLE sdm2_log.sdm_event_target (
    -- metadata：稳定控制、追溯与版本字段
    tenant_id                     VARCHAR(128) NOT NULL,
    occur_time                    DATETIME(3) NOT NULL,
    event_id                      VARCHAR(128) NOT NULL,
    log_id                        VARCHAR(128) NULL,
    ingest_time                   DATETIME(3) NULL,
    parse_time                    DATETIME(3) NULL,
    raw_log_id                    VARCHAR(128) NULL,
    source_original_event_id      VARCHAR(128) NULL,
    schema_version                INT NULL,
    mapping_id                    VARCHAR(128) NULL,
    mapping_revision              VARCHAR(64) NULL,
    projection_version            VARCHAR(64) NULL,
    quality_status                VARCHAR(32) NULL,

    data_src_vendor               VARCHAR(128) NULL,
    data_src_product              VARCHAR(128) NULL,
    data_src_category             VARCHAR(128) NULL,
    data_src_instance_id          VARCHAR(128) NULL,
    log_type                      VARCHAR(128) NULL,
    log_level                     VARCHAR(64) NULL,
    log_name                      VARCHAR(255) NULL,

    -- event：稳定分类、动作和有界摘要
    record_kind                   VARCHAR(32) NULL,
    event_domain                  VARCHAR(64) NULL,
    event_type                    VARCHAR(128) NULL,
    operation                     VARCHAR(64) NULL,
    outcome                       VARCHAR(64) NULL,
    severity                      VARCHAR(64) NULL,
    log_content                   VARCHAR(4096) NULL COMMENT '有界事件摘要，不保存完整原文',

    -- roles：主值或高频检索投影
    source_ip                     VARCHAR(64) NULL,
    source_port                   INT NULL,
    source_mac                    VARCHAR(64) NULL,
    source_user                   VARCHAR(255) NULL,
    source_account                VARCHAR(255) NULL,
    source_host                   VARCHAR(255) NULL,
    source_process_name           VARCHAR(255) NULL,

    carrier_process_name          VARCHAR(255) NULL,
    carrier_process_pid           VARCHAR(64) NULL,
    carrier_process_guid          VARCHAR(128) NULL,

    target_ip                     VARCHAR(64) NULL,
    target_port                   INT NULL,
    target_mac                    VARCHAR(64) NULL,
    target_user                   VARCHAR(255) NULL,
    target_host                   VARCHAR(255) NULL,
    target_service                VARCHAR(255) NULL,
    target_domain                 VARCHAR(255) NULL,
    target_url                    VARCHAR(8192) NULL COMMENT '有界 URL 查询投影',
    target_file_path              VARCHAR(4096) NULL COMMENT '有界文件路径查询投影',
    target_file_name              VARCHAR(1024) NULL,
    target_file_sha256            VARCHAR(128) NULL,

    observer_vendor               VARCHAR(128) NULL,
    observer_product              VARCHAR(128) NULL,
    observer_type                 VARCHAR(128) NULL,
    device_ip                     VARCHAR(64) NULL,

    -- facets：跨来源稳定且已证明需要查询的投影
    network_protocol              VARCHAR(64) NULL,
    network_direction             VARCHAR(64) NULL,
    network_session_id            VARCHAR(128) NULL,
    application_name              VARCHAR(255) NULL,
    http_method                   VARCHAR(32) NULL,
    http_status                   INT NULL,
    http_host                     VARCHAR(255) NULL,
    k8s_namespace                 VARCHAR(128) NULL,
    k8s_pod_name                  VARCHAR(255) NULL,

    -- source_finding：过滤、列表和证据回溯投影
    source_finding_title          VARCHAR(1024) NULL,
    source_finding_severity       VARCHAR(64) NULL,
    source_finding_category       VARCHAR(128) NULL,
    source_finding_signature_id   VARCHAR(128) NULL,
    source_finding_action         VARCHAR(128) NULL,
    source_finding_original_id    VARCHAR(128) NULL,

    -- 完整逻辑权威对象；缺失对象写 SQL NULL，不写空对象占位
    roles_obj                     VARIANT NULL COMMENT 'roles 逻辑权威对象（物理列名规避 Doris 保留字）',
    facets_obj                    VARIANT NULL COMMENT 'facets 逻辑权威对象',
    source_finding_obj            VARIANT NULL COMMENT 'source_finding 逻辑权威对象',
    extensions_obj                VARIANT NULL COMMENT 'extensions 逻辑权威对象',

    INDEX idx_record_kind (record_kind) USING INVERTED,
    INDEX idx_event_domain (event_domain) USING INVERTED,
    INDEX idx_event_type (event_type) USING INVERTED,
    INDEX idx_source_ip (source_ip) USING INVERTED,
    INDEX idx_target_ip (target_ip) USING INVERTED,
    INDEX idx_source_user (source_user) USING INVERTED,
    INDEX idx_target_host (target_host) USING INVERTED,
    INDEX idx_target_domain (target_domain) USING INVERTED,
    INDEX idx_observer_product (observer_product) USING INVERTED,
    INDEX idx_finding_severity (source_finding_severity) USING INVERTED,
    INDEX idx_finding_signature (source_finding_signature_id) USING INVERTED
)
UNIQUE KEY(tenant_id, occur_time, event_id)
PARTITION BY RANGE(occur_time) ()
DISTRIBUTED BY HASH(tenant_id, event_id) BUCKETS 16
PROPERTIES (
    "enable_unique_key_merge_on_write" = "true",
    "replication_num" = "1",
    "dynamic_partition.enable" = "true",
    "dynamic_partition.time_unit" = "DAY",
    "dynamic_partition.time_zone" = "Etc/UTC",
    "dynamic_partition.start" = "-90",
    "dynamic_partition.end" = "3",
    "dynamic_partition.prefix" = "p",
    "dynamic_partition.buckets" = "16",
    "bloom_filter_columns" = "event_id,log_id,raw_log_id,source_original_event_id",
    "inverted_index_storage_format" = "V3",
    "light_schema_change" = "true"
);
~~~

上述索引是基线候选，不是“所有平铺列都建索引”。`source_port/target_port/http_status` 等数值列默认依靠分区裁剪和列扫描；`target_url/target_file_path/log_content` 等长文本默认用于详情或摘要。新增投影列必须同时满足：语义稳定、跨来源通用、存在明确的过滤、聚合、关联或列表展示需求。示例中的单副本仅用于与当前 POC 基线对齐，生产副本数必须按可用性、容量和恢复目标单独评审。

完整 `roles/facets/source_finding` 保留被平铺字段，因此存在受控的物理冗余。冗余只允许由同一个 projection pipeline 生成。多 carrier、related、任意收件人、附件、IOC、CVE 和 ATT&CK 等需要按任意成员反向检索的数组，必须派生到实体 posting 或窄关系表，不能依赖主值列或在线扫描整个 `VARIANT`。

### 13.2 字段类型约束

| 字段类别 | 当前类型 | 结论与约束 |
|---|---|---|
| 时间 | `DATETIME(3)` | 合理；接入前统一时区，分区时间必须使用 `occur_time` |
| IP | `VARCHAR(64/128)` | 兼容可保留；写入前必须规范化文本，禁止同一地址多种表示 |
| 端口、HTTP 状态 | `INT` | 合理；接入层校验端口 `0..65535`、HTTP 状态合法范围 |
| 哈希 | `VARCHAR(128)` | 兼容可保留；按算法校验长度并统一小写十六进制 |
| URL、命令行、SQL、消息 | `TEXT` 或有界 `VARCHAR` | 适合展示或取证摘要，不作为默认精确过滤列；查询投影必须定义最大写入长度、截断标记和敏感信息策略 |
| 扩展 | `VARIANT` | 适合稀疏低频字段；必须遵循第十章 envelope，不承担高频查询 |

`log_content` 只保存摘要，不复制完整原文。URL、命令行、SQL、邮件主题等字段应按日志类型定义长度上限；发生截断时保留 `raw_log_id` 并输出数据质量状态。

### 13.3 索引与点查

现有倒排索引适合 `event_type`、source/target、observer、K8s 等高频过滤。P0 应通过真实查询样本评估以下候选：

| 查询字段 | 建议 | 前提 |
|---|---|---|
| `record_kind`、`event_domain` | INVERTED | 必选分类维度 |
| `source_finding_signature_id`（兼容名 `source_alert_signature_id`） | INVERTED | 规则、签名聚合与过滤频繁 |
| `device_ip` | INVERTED | 经常按探针或设备筛选 |
| `log_id`、`raw_log_id`、`source_finding_original_id`（兼容名 `source_alert_original_id`） | Bloom Filter 或 INVERTED | 存在稳定的事件反查需求 |

不能只凭字段存在就创建索引。上线前用目标数据量、基数、时间范围和查询模板验证存储放大、写入开销和 P95 延迟；事件查询仍应尽量携带 `occur_time` 范围以触发分区裁剪。

### 13.4 VARIANT 与容量边界

四个 `VARIANT` 必须分别定义 Schema、缺失字段策略、路径数量、嵌套深度、单对象字节上限和超限处理：

- 缺失对象写 SQL `NULL`，对象内部省略缺失属性，不批量写空字符串、`null` 和空对象；
- `roles`、`facets`、`source_finding` 不保存完整原始日志，也不复制彼此的标准对象；
- `extensions` 必须遵循固定 envelope，禁止复制完整 `roles/facets/source_finding`，禁止内联 Payload、HTTP Body、邮件正文和 PCAP；
- URL、命令行、SQL、消息等长值只能有一个完整权威副本，顶层列如需列表展示应保存有界值或摘要并记录截断状态；
- 原始内容外置，并由 `tenant_id + raw_log_id` 稳定回查。

在缺少真实分布数据前，P0 可采用以下单事件写入护栏作为基准起点：`roles ≤ 16 KiB`、`facets ≤ 32 KiB`、`source_finding ≤ 32 KiB`、`extensions ≤ 64 KiB`，四者合计不超过 `128 KiB`。超限对象不得静默截断；写入器应保留外部稳定引用，将 `quality_status` 标记为超限，并输出可统计的数据质量记录。最终阈值必须根据真实样本分位数和 Doris 基准调整。

容量评审不能只使用 JSONL 输入大小。1M 和 10M 基准必须在 Compaction 稳定后分别采集主数据、倒排索引、Bloom Filter、各分区和副本的 Doris 实际落盘大小，并报告：

~~~text
每百万事件输入字节
每百万事件单副本 data_size / index_size / total_size
VARIANT 占比与顶层投影重复字节
压缩比、写入吞吐、Compaction 时间和峰值内存
核心查询 p50 / p95 / p99 与扫描字节
~~~

---

## 十四、实施顺序

### P0：固化逻辑契约

1. 为五层结构建立 JSON Schema 或等价机器可执行 Schema；
2. 为 87 个字段建立逻辑路径到物理列的 projection registry；
3. 定义 `roles/facets/source_finding/extensions` 四个 `VARIANT` 的 Schema、容量和缺失字段策略；
4. 增加并索引 `record_kind/event_domain`，修正 `event_category=alert` 的维度混用；
5. 增加 `projection_version/quality_status`，明确映射版本与物理投影版本的边界；
6. 明确 `device/observer`、`carrier/facet`、`URL/HTTP` 和 source Finding 的权威归属。

### P1：建立事件类型契约

1. 从高频 `event.type` 开始定义路径必要性和角色约束；
2. 将现有日志源 mapping manifest 绑定到事件类型契约；
3. 验证同类事件跨产品生成相同逻辑结构；
4. 输出逻辑完整率、物理投影完整率和未映射字段报告。

### P2：补齐多值与稳定对象域

1. 增加 related、多个 carrier、邮件附件、IOC、漏洞和证据关系；
2. 为 DNS、TLS、认证、注册表、云、容器和数据库等稳定 facet 建立 Schema；
3. 对需要反向检索的数组建立窄明细表或实体索引；
4. 将历史 `extension` 按 source-private、profile、enrichment 和 unmapped 分类治理。

### P3：提供兼容接口

1. Doris 宽表接口继续服务现有查询；
2. 新增五层逻辑事件 API 或导出视图；
3. 基于同一来源映射生成 SDM 内部投影和 OCSF 交换投影；
4. 对字段增删、类型变化、投影变化和 Profile 变化执行版本化兼容评审。

---

## 十五、验收标准

| 维度 | 验收要求 |
|---|---|
| 当前字段覆盖 | 87 个物理字段均有明确逻辑落位或来源契约约束 |
| 计数一致性 | `15 + 6 + 36 + 23 + 6 + 1 = 87` |
| 目标分类投影 | 新数据同时写入 `record_kind` 和 `event_domain`，不再依赖 `event_category` 混合表达 |
| 权威归属 | 每个标准事实只有一个 canonical logical path |
| 投影可解释 | 每个物理列声明投影类型和来源路径 |
| 投影一致性 | `roles/facets/source_finding` 与顶层投影由同一 pipeline 生成，并按 `projection_version` 校验 |
| 角色一致性 | 同一事件类型跨产品遵循同一角色规则 |
| 多值保真 | 主表只投影主值，完整 carrier、related、收件人、附件由逻辑对象或关系表保留 |
| 扩展治理 | `extensions` 遵循固定 envelope，并具有命名空间、类型、版本、大小限制和生命周期 |
| 原始可追溯 | 可通过 `tenant_id + raw_log_id` 回查原始数据 |
| 告警边界 | source Finding 不携带平台 Alert 工作流状态 |
| 性能边界 | 高频查询继续使用物理列和索引，不强制扫描完整嵌套 JSON |
| 容量边界 | 1M/10M 基准分别报告 data/index/total size、VARIANT 占比、压缩比和每百万事件成本 |

---

## 十六、与现有文档的关系

| 文档 | 关系 |
|---|---|
| `docs/4.SDM2.0-日志模型概念研究.md` | 提供 Event、五角色、实体和 source_alert 的概念基础 |
| `log-model/docs/main/05-sdm-event-logical-field-catalog.md` | 当前 87 个标准物理字段的规范性字典 |
| `log-model/contracts/hybrid-event/projection-registry.v1.json` | 当前字段机器可读元数据 |
| `log-model/schema/007_sdm_event.sql` | 当前 Doris 物理表最终可执行契约 |
| `docs/9.SDM2.0-与OCSF对比分析.md` | 提供 OCSF 兼容、Source Finding 和分层建模依据 |

本文补齐的是上述概念模型与物理字段标准之间的逻辑对象和投影层。`log-model/docs/main/05-sdm-event-logical-field-catalog.md` 是规范性来源；`log-model/docs` 下的同名文件只能作为生成镜像，不得独立修改，CI 应校验两者一致。后续若进入实施阶段，应通过 OpenSpec 正式定义 Schema、projection registry、DDL 迁移、兼容策略、生成器和验收测试。
